({
    doInit : function(component, event, helper) {
     /*   var actionGetQuote = component.get("c.getQuoteDetails");
        actionGetQuote.setParams({
            "quoteId": component.get("v.recordId")
        });
        actionGetQuote.setCallback(this, function(response) {
            var retVal = response.getReturnValue();
            if(retVal != null)
            {
                component.set("v.quoteRec",retVal);
            }
        }); 
        $A.enqueueAction(actionGetQuote);
       */ 
    },
})